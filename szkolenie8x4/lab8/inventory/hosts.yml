---
# Inventory labu 8 — dwa LOKALNE hosty (ansible_connection: local), zeby
# pokazac delegate_to i run_once na >1 hoscie bez potrzeby zdalnych maszyn.
all:
  children:
    appservers:
      hosts:
        app-a:
          ansible_connection: local
        app-b:
          ansible_connection: local
