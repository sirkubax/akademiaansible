---
# Skopiuj lub podlinkuj dane z lab0_w1, albo uzupelnij ponownie
all:
  vars:
    ansible_user: ubuntu
    ansible_ssh_private_key_file: ~/.ssh/id_rsa

  children:
    webservers:
      hosts:
        web01:
          ansible_host: 1.2.3.4    # <-- uzupelnij
        web02:
          ansible_host: 1.2.3.5    # <-- uzupelnij
